# URL of the middleware (FastAPI) service.
MIDDLEWARE_URL=http://localhost:8000

# When true, the frontend uses fake data and never calls the middleware.
# Set to false once the middleware is running. Nothing else needs to change.
USE_FAKE_DATA=true
